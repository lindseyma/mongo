from pymongo import MongoClient
import csv

f = open("peeps.csv")
d = csv.DictReader(f)


server = MongoClient('127.0.0.1')

db = server.mongo

c = db.students

for thing in d:
    p = {'name': thing['name'] , 'age': thing['age'], 'id': thing['id']}
    c.insert_one(p)
